/**
 * Server-authoritative optional-mod adapters. Classes in this package may depend on a target
 * mod only behind a Forge loaded-mod check; they translate third-party state into Territory
 * Control APIs and must fail closed when an applicable entity cannot be classified.
 */
package com.arxyt.territorycontrolcompat.compat;
